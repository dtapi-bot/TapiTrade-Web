# ==========================================================
#  Fichiers INTERNES — ne JAMAIS publier sur le site
# ==========================================================

# Tout le dossier de travail interne :
#   internal/flyers   → flyers & miniatures (sources .html + exports .png)
#   internal/docs     → doc-indices (.html + .pdf)
#   internal/outreach → listes de contacts WhatsApp / Calendly (RGPD)
#   internal/workshop → suivi interne (mots de passe) + emails marketing
internal/

# --- QR codes (restent dans assets/, utilisés uniquement par les flyers) ---
assets/qr-discord.png
assets/qr-masterclass.png
